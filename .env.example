# Loom Agent — environment variables (optional)
#
# Setup:
#   cp .env.example .env
#   npm run dev

# OpenAI Whisper API (Review → Transcription → OpenAI API)
OPENAI_API_KEY=

# Optional: Python that has `pip install openai-whisper` (if not on default PATH)
# WHISPER_PYTHON=/Library/Frameworks/Python.framework/Versions/3.10/bin/python3
